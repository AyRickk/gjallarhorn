# Server Configuration
HOST=0.0.0.0
PORT=8080

# CORS Configuration (comma-separated list of allowed origins)
# CRITICAL: Set this for production! Empty value uses permissive CORS (NOT RECOMMENDED)
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:3000

# Database Configuration
DATABASE_URL=postgresql://feedback_user:feedback_pass@localhost:5432/feedback_db

# Keycloak Configuration
KEYCLOAK_URL=http://localhost:8180/realms/master
KEYCLOAK_REALM=master
# Optional: for token validation caching
KEYCLOAK_JWKS_CACHE_TTL=3600

# Webhooks Configuration (comma-separated URLs)
WEBHOOK_URLS=http://localhost:8081/webhook

# Export Configuration
EXPORT_MAX_RECORDS=10000

# Monitoring
RUST_LOG=info,feedback_api=debug
