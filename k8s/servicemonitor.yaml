apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: feedback-api
  namespace: feedback-system
  labels:
    app: feedback-api
spec:
  selector:
    matchLabels:
      app: feedback-api
  endpoints:
  - port: http
    path: /metrics
    interval: 30s
    scrapeTimeout: 10s
